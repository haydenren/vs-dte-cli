# vs-dte-cli

Visual Studio DTE automation CLI for .NET Framework MSTest debugging.

This repository is a public-extraction workspace. The initial source was copied
from an internal automation repository and still needs public cleanup before it
is published:

- remove repository-specific defaults
- remove internal test names and paths
- rename internal live-debug environment variables
- add public usage examples
- choose and add a license

Current source lives under `src/VsDteCli/`.
